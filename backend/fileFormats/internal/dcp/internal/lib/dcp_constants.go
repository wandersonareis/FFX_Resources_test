package lib

const DCP_FILE_BINARY_PARTS_PATTERN string = dcp_file_pattern + "$"
const DCP_FILE_TXT_PARTS_PATTERN string = dcp_file_pattern + `\.txt`
const DCP_PARTS_TARGET_DIR_NAME string = "system_text"
const dcp_file_pattern string = `macrodic\.(00[0-9]|01[0-5])`
